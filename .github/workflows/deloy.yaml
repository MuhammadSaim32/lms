name: test ci cd labled         # optional, just a label

on:                         # WHEN to trigger
  push:
    branches:
      - main

jobs:             
  job-name: 
    runs-on: ubuntu-latest  
    steps:                  
      - name: Step 1 name
        run: echo "SSRF"